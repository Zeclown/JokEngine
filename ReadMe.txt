To run (and maybe compile) this code, you will need to install OpenAL
It currently only compiles in Debug on x86
JoustGame.zip contains the compiled exemple game. It still requires OpenAL (https://openal.org/)